#include <iostream>
using namespace std;
int main()
{
    int a = 45.33;
    cout << "the value of a is " << (float)a << endl;
    cout << "the value of a is " << (int)a << endl;
    cout << "the value of a is " << (double)a << endl;
    cout << "the value of a is " << (double long)a << endl;
    cout << "the value of a is " << float(a) << endl; //(a)this is also work same like how i use uper side
}